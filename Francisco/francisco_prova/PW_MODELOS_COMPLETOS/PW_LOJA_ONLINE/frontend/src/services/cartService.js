const KEY='pw_store_cart';export function getCart(){try{return JSON.parse(localStorage.getItem(KEY))||[]}catch{return[]}}export function setCart(c){localStorage.setItem(KEY,JSON.stringify(c));window.dispatchEvent(new Event('cart-change'));return c}export function addToCart(p,qty=1){const c=getCart();const old=c.find(x=>x.productId===p.id);if(old)old.quantity=Math.min(old.quantity+qty,p.stock);else c.push({productId:p.id,name:p.name,price:p.price,stock:p.stock,quantity:Math.min(qty,p.stock)});return setCart(c)}export function clearCart(){setCart([])}