import {useEffect,useState} from 'react';import {orders,setOrderStatus} from '../services/storeService';const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const sts=['PENDING','PAID','PROCESSING','SHIPPED','DELIVERED','CANCELED'];export default function OrdersPage({user}){const[list,setList]=useState([]);async function load(){setList(await orders())}useEffect(()=>{load()},[]);return <main className="page-content"><h1>{user.role==='ADMIN'?'Todos os pedidos':'Meus pedidos'}</h1><section className="card"><div className="table-wrap"><table><thead><tr><th>#</th><th>Data</th><th>Cliente</th><th>Itens</th><th>Total</th><th>Status</th></tr></thead><tbody>{list.map(o=><tr key={o.id}><td>{o.id}</td><td>{new Date(o.createdAt).toLocaleString()}</td><td>{o.customerName}<div className="muted">{o.userEmail}</div></td><td>{o.items.map(i=>`${i.quantity}× ${i.productName}`).join(', ')}</td><td>{money(o.total)}</td><td>{user.role==='ADMIN'?<select value={o.status} onChange={async e=>{await setOrderStatus(o.id,e.target.value);load()}}>{sts.map(s=><option key={s}>{s}</option>)}</select>:<span className="badge badge-info">{o.status}</span>}</td></tr>)}</tbody></table></div></section></main>}