import {useEffect,useState} from 'react';import {dashboard} from '../services/gestaoService';
const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export default function DashboardPage({user}){const[d,setD]=useState(null);const[e,setE]=useState('');async function load(){try{setD(await dashboard())}catch(x){setE('Erro ao carregar dashboard')}}useEffect(()=>{load()},[]);return <main className="page-content"><section className="hero"><h1>Gestão Empresarial</h1><p>Estoque + vendas + financeiro integrados. Perfil atual: {user?.role}.</p></section>{e&&<div className="error-box">{e}</div>}{d&&<><section className="stats-grid"><article className="stat-card"><span>Produtos</span><strong>{d.products}</strong><small>{d.stockUnits} unidades em estoque</small></article><article className="stat-card"><span>Estoque baixo</span><strong className="warning">{d.lowStock}</strong><small>No mínimo ou abaixo</small></article><article className="stat-card"><span>Vendas</span><strong>{d.sales}</strong><small>Pedidos registrados</small></article><article className="stat-card"><span>Saldo financeiro</span><strong className={Number(d.balance)>=0?'positive':'negative'}>{money(d.balance)}</strong><small>{money(d.income)} entradas • {money(d.expense)} saídas</small></article></section><section className="card"><h2>Fluxo integrado de exemplo</h2><p>Ao registrar uma venda, o backend valida o estoque, baixa os produtos, cria o histórico de movimentação e lança automaticamente uma receita no financeiro.</p></section></>}</main>}
