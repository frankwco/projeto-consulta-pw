import React,{useEffect,useState} from 'react';import {Users,FileText,MessageCircle,Heart,UserPlus} from 'lucide-react';import {api,errorMessage} from '../api/api';
export default function DashboardPage(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{api.get('/dashboard').then(r=>setData(r.data)).catch(e=>setError(errorMessage(e)))},[]);if(error)return <section className="page"><p className="error">{error}</p></section>;if(!data)return <section className="page"><div className="card empty">Carregando...</div></section>;const cards=[[Users,'Usuários',data.users],[FileText,'Posts',data.posts],[MessageCircle,'Comentários',data.comments],[Heart,'Curtidas',data.likes],[UserPlus,'Conexões',data.follows]];return <section className="page"><div className="page-title"><div><h1>Dashboard</h1><p>Visão administrativa da rede.</p></div></div><div className="stats-grid">{cards.map(([Icon,label,value])=><article className="card stat" key={label}><Icon/><div><span>{label}</span><strong>{value}</strong></div></article>)}</div></section>}
