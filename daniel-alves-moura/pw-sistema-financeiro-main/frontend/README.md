# Frontend

Frontend React do sistema financeiro.

```bash
npm install
npm start
```

API configurada em `.env`:

```env
REACT_APP_API_BASE_URL=http://localhost:8080
```

O visual reutiliza o CSS original do projeto: azul `#003345`, cinzas e cores sólidas, sem gradientes.
